import user from './modules/user';

// const files = require.context('./modules', true, /\.js/);
// console.log(files.keys(), 90);
// const apis = {};
// files.keys().forEach(key => {
//   const api = files(key).default;
//   console.log(files(key), 765);
// });
export default {
  user
};
